---
summary: "CLI reference for `codyai clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `codyai clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `codyai clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `codyai clawbot qr` (same behavior as [`codyai qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `codyai clawbot qr` -> `codyai qr`
